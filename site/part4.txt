/photo-1497250681960-ef046c08a56e?auto=format&fit=crop&w=1200&q=85','BLUE HOUR','COMMERCIAL · COLOR GRADING'],
['https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?auto=format&fit=crop&w=1200&q=85','CHRISTMAS HOUSE','BRAND FILM · COLOR'],
['https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1200&q=85','FROZEN LIGHT','NARRATIVE · LOOK DEVELOPMENT']];
const gallery=document.querySelector('.gallery'),track=document.querySelector('.track'),title=document.querySelector('.title h2'),sub=document.querySelector('.sub'),index=document.querySelector('.index');[...items,...items,...items].forEach((it,i)=>{const c=document.createElement('article');c.className='card';c.innerHTML='<img src="'+it[0]+'" alt="'+it[1]+'"><span class="num">'+String(i%5+1).padStart(2,'0')+'</span>';track.appendChild(c)});const cards=[...document.querySelectorAll('.card')];let cur=5,target=5,drag=false,sx=0,st=0;const step=()=>cards[0].getBoundingClientRect().width+28;const norm=()=>{while(target<2.5)target+=5;while(target>12.5)target-=5};function info(){let a=((Math.round(cur)%5)+5)%5;title.textContent=items[a][1];sub.textContent=items[a][2];index.textContent=String(a+1).padStart(2,'0')+' / 05';cards.forEach((c,i)=>c.classList.toggle('active',i===Math.round(cur)+5))}function loop(){cur+=(target-cur)*.11;track.style.transform='translate3d('+(gallery.clientWidth/2-(cur+.5)*step())+'px,0,0)';info();requestAnimationFrame(loop)}gallery.addEventListener('wheel',e=>{e.preventDefault();target+=e.deltaY>0||e.deltaX>0?1:-1;norm()},{passive:false});gallery.addEventListener('pointerdown',e=>{drag=true;sx=e.clientX;st=target;gallery.setPointerCapture(e.pointerId)});gallery.addEventListener('pointermove',e=>{if(drag){target=st-(e.clientX-sx)/step();norm()}});gallery.addEventListener('pointerup',()=>{drag=false;target=Math.round(target);norm()});gallery.addEventListener('keydown',e=>{if(e.key==='ArrowRight')target++,norm();if(e.key==='ArrowLeft')target--,norm()});loop();
</script></body></html>